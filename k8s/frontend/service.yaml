apiVersion: v1
kind: Service
metadata:
  name: jerney-frontend
  namespace: jerney
spec:
  selector:
    app: jerney-frontend
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
